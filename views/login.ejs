<!DOCTYPE html>
<html lang="es" class="h-full bg-gray-50">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Acceso | Empresa</title>

  <!-- Tailwind (CDN de referencia). Para producción recomiendo compilar local. -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <!-- Fuente y pequeños ajustes -->
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    html { font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, sans-serif; }
    /* Sutil patrón de fondo corporativo */
    .bg-grid {
      background-image:
        radial-gradient(circle at 1px 1px, rgba(0,0,0,.035) 1px, transparent 0);
      background-size: 24px 24px;
    }
  </style>
</head>

<body class="h-full">
  <div class="min-h-screen flex">
    <!-- Panel de marca (oculto en móviles) -->
    <aside class="hidden lg:flex w-1/2 flex-col justify-between p-12 bg-white">
      <header class="flex items-center space-x-3">
        <!-- Logo simple (SVG placeholder) -->
        <svg class="w-10 h-10 text-blue-600" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <path d="M6 3h12a1 1 0 011 1v16a1 1 0 01-1 1H6a1 1 0 01-1-1V4a1 1 0 011-1zm2 4v10h8V7H8z"/>
        </svg>
        <img src="resources/img/Logo.png" alt="Logo de la empresa" class="w-10 h-10 text-blue-600">
        <span class="text-xl font-semibold text-gray-900 tracking-tight">Refther</span>
      </header>

      <div class="flex-1 flex items-center">
        <div>
          <h1 class="text-3xl font-semibold text-gray-900 leading-snug">Bienvenido</h1>
          <p class="mt-3 text-gray-600 max-w-md">
            Accede a tu panel para gestionar operaciones de forma segura y eficiente.
          </p>
        </div>
      </div>

      <footer class="text-xs text-gray-400">
        © <span id="year"></span> RodSoft. Todos los derechos reservados.
      </footer>
    </aside>

    <!-- Columna del formulario -->
    <main class="w-full lg:w-1/2 flex items-center justify-center px-6 py-12 bg-gray-50 bg-grid">
      <div class="w-full max-w-md">
        <div class="mb-8 flex items-center lg:hidden">
          <svg class="w-8 h-8 text-blue-600" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M6 3h12a1 1 0 011 1v16a1 1 0 01-1 1H6a1 1 0 01-1-1V4a1 1 0 011-1zm2 4v10h8V7H8z"/>
          </svg>
          <span class="ml-2 text-lg font-semibold text-gray-900">Refther</span>
        </div>

        <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
          <h2 class="text-2xl font-semibold tracking-tight text-gray-900">Inicia sesión</h2>
          <p class="mt-1 text-sm text-gray-500">Usa tu correo corporativo y contraseña.</p>

          <form class="mt-8 space-y-5" action="/auth" method="post" novalidate>
            <!-- Ejemplo CSRF si lo usas -->
            <!-- <input type="hidden" name="_csrf" value=""> -->

            <div>
              <label for="user" class="block text-sm font-medium text-gray-700">Correo</label>
              <input
                type="email"
                name="user"
                id="user"
                inputmode="email"
                autocomplete="email"
                required
                placeholder="tucorreo@empresa.com"
                class="mt-2 block w-full rounded-lg border border-gray-200 px-3 py-2.5 text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
              />
            </div>

            <div>
              <label for="pass" class="block text-sm font-medium text-gray-700">Contraseña</label>
              <div class="mt-2 relative">
                <input
                  type="password"
                  name="pass"
                  id="pass"
                  autocomplete="current-password"
                  required
                  minlength="6"
                  placeholder="••••••••"
                  class="block w-full rounded-lg border border-gray-200 px-3 py-2.5 pr-11 text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
                />
                <button type="button" id="togglePass"
                  class="absolute inset-y-0 right-0 px-3 text-gray-400 hover:text-gray-600 focus:outline-none"
                  aria-label="Mostrar u ocultar contraseña">
                  <!-- ojito -->
                  <svg id="icon-eye" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z" stroke-width="1.5"/>
                    <circle cx="12" cy="12" r="3" stroke-width="1.5"/>
                  </svg>
                </button>
              </div>
            </div>

            <div class="flex items-center justify-between">
              <label class="inline-flex items-center space-x-2 text-sm text-gray-600 cursor-pointer select-none">
                <input type="checkbox" name="remember" class="rounded border-gray-300 text-blue-600 focus:ring-blue-500">
                <span>Recordarme</span>
              </label>
              <a href="/recuperar" class="text-sm font-medium text-blue-600 hover:text-blue-700">¿Olvidaste tu contraseña?</a>
            </div>

            <button
              type="submit"
              class="w-full inline-flex items-center justify-center rounded-lg bg-blue-600 text-white font-medium px-4 py-2.5 hover:bg-blue-700 focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition"
            >
              Entrar
            </button>

            <!-- Acciones secundarias opcionales -->
            <div class="pt-2 text-center text-sm">
              ¿Sin cuenta?
              <a href="/register" class="font-medium text-blue-600 hover:text-blue-700">Crear una</a>
            </div>
          </form>
        </div>

        <!-- Pequeño texto legal -->
        <p class="mt-6 text-xs text-center text-gray-400">
          Al continuar aceptas las políticas de uso y privacidad.
        </p>
      </div>
    </main>
  </div>

  <!-- SweetAlert2 + bloque EJS que ya utilizas -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <% if (typeof alert !== "undefined") { %>
  <script>
    Swal.fire({
      title: '<%= alertTitle %>',
      text: '<%= alertMessage %>',
      icon: '<%= alertIcon %>',
      showConfirmButton: <%= showConfirmButton === true %>,
      timer: <%= typeof timer !== 'undefined' ? timer : 'null' %>
    }).then(() => {
      window.location = '/<%= ruta %>';
    });
  </script>
  <% } %>

  <!-- UX: año dinámico + toggle contraseña -->
  <script>
    document.getElementById('year').textContent = new Date().getFullYear();

    const pass = document.getElementById('pass');
    const toggle = document.getElementById('togglePass');
    const eye = document.getElementById('icon-eye');
    toggle?.addEventListener('click', () => {
      const visible = pass.type === 'text';
      pass.type = visible ? 'password' : 'text';
      // Cambia el ícono de forma simple
      eye.innerHTML = visible
        ? '<path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z" stroke-width="1.5"/><circle cx="12" cy="12" r="3" stroke-width="1.5"/>'
        : '<path d="M17.94 17.94A10.94 10.94 0 0112 19c-7 0-11-7-11-7a20.68 20.68 0 015.06-5.94M9.88 9.88A3 3 0 0012 15a3 3 0 002.12-.88M1 1l22 22" stroke-width="1.5"/>';
    });
  </script>
</body>
</html>
